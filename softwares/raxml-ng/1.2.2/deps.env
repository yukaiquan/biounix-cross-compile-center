# raxml-ng 依赖配置

# Linux (Ubuntu)
DEPS_APT="build-essential cmake flex bison libgmp-dev libtbb-dev unzip"

# macOS
DEPS_BREW="cmake flex bison gmp tbb unzip"

# Windows (MSYS2)
# Windows 不支持: pll-modules 子模块与 GCC 15 不兼容
# 请从 https://github.com/amkaze/raxml-ng/releases 下载预编译版本
DEPS_MSYS2=""
