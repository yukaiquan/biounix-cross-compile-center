# LDBlockShow v1.41

## 工具简介
LDBlockShow - 用于可视化连锁不平衡的工具（与 PopLDdecay 类似）

## 依赖
- g++ (C++11)
- zlib
- Perl (SVG.pm)

## 编译说明
- 使用 configure + make 或直接编译 src/*.cpp
- Windows 需要 MinGW 交叉编译环境

## 输出
- `LDBlockShow` (Linux/macOS)
- `LDBlockShow.exe` (Windows)
