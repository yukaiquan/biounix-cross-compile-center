# bwa-mem2 依赖配置
# 仅支持 Linux x86_64

# Linux x86_64
DEPS_APT="build-essential zlib1g-dev"

# macOS - 不支持
# bwa-mem2 与新版 macOS SDK 不兼容（safestringlib 冲突）

# Windows x64 - 可能支持
DEPS_MSYS2="mingw-w64-x86_64-gcc mingw-w64-x86_64-zlib"
